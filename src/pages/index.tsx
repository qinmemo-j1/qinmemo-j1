import React from "react";
import type { NextPage } from "next";

import { Layout } from "@/layouts";

const Home: NextPage = () => {
  return (
    <Layout>
      <div>tasks</div>
    </Layout>
  );
};

export default Home;
